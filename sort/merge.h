/// \file merge.h
/// \brief merge sort
/// \author juhyun-nam

#ifndef SORT_MERGE_H_
#define SORT_MERGE_H_

#include <iterator>

namespace ma {

/*
template <
    typename It,
    typename Comp = std::less<typename std::iterator_traits<It>::value_type>>
inline void MergeSort(It first, It last, Comp comp = Comp()) {
  auto length = last - first;
  detail::SliceAndSort(first, last);
  detail::Merge(first, last);
}
*/

}  // namespace ma

#endif  // SORT_MERGE_H_
